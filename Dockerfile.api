FROM node:22-alpine
WORKDIR /app
COPY package*.json ./
RUN npm ci --omit=dev
COPY server/ ./server/
COPY src/db/local.ts ./src/db/local.ts
RUN mkdir -p /data
ENV DB_PATH=/data/adhd.db
ENV PORT=3001
EXPOSE 3001
CMD ["node", "--import", "tsx/esm", "server/index.ts"]
